# Secrets — NEVER commit these
.env
*.key
*.pem

# Runtime data
positions.json
*.jsonl
arb_trades.csv
*.log
__pycache__/
*.pyc
.pytest_cache/

# Local environment
venv/
.claude/

# Weather climatology (regenerate with scripts/build_weather_climo.py)
data/weather_climo.json

# Belt-and-suspenders: ignore any .env variant (backups etc.)
.env*
*.pid
